{
  "name": "@open-los/conformance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:smoke": "vitest run --testPathPattern smoke",
    "test:conformance": "vitest run"
  },
  "dependencies": {
    "@open-los/api": "*",
    "@open-los/core": "*",
    "yaml": "^2.7.0",
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.0",
    "jsonpath-plus": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
